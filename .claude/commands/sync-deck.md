---
description: Sync the slide deck with the current state of the prototype and flow doc.
---

Invoke the `presentation-updater` subagent. After it finishes, remove `presentation/.stale` if it exists.
